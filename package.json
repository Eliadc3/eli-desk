{
  "name": "eli-desk",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm run db:init",
    "db:init": "npm --prefix backend run prisma:generate && npm --prefix backend run prisma:migrate && npm",
    "dev": "concurrently -n backend,frontend -c auto \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "migrate": "npm --prefix backend run prisma:migrate"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}